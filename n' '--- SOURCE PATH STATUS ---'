[1mdiff --git a/build-anland.sh b/build-anland.sh[m
[1mindex d9ed0706..1096b23d 100755[m
[1m--- a/build-anland.sh[m
[1m+++ b/build-anland.sh[m
[36m@@ -44,8 +44,8 @@[m [mEOF[m
         libgbm-dev libgles-dev libglaze-dev libhyprcursor-dev libhyprgraphics-dev \[m
         libhyprlang-dev libhyprutils-dev libhyprwire-dev libinput-dev liblcms2-dev \[m
         liblua5.5-dev libmuparser-dev libpango1.0-dev libpixman-1-dev \[m
[31m-        libpipewire-0.3-dev libspa-0.2-dev libseat-dev libre2-dev libssl-dev \[m
[31m-        libtomlplusplus-dev libudev-dev libudis86-dev libwayland-dev libxkbcommon-dev \[m
[32m+[m[32m        libpipewire-0.3-dev libspa-0.2-dev libseat-dev libeis-dev libre2-dev libssl-dev \[m
[32m+[m[32m        libsdbus-c++-dev libtomlplusplus-dev libudev-dev libudis86-dev libwayland-dev libxkbcommon-dev \[m
         libxcb1-dev libxcb-render0-dev libxcb-xfixes0-dev libxcb-icccm4-dev \[m
         libxcb-composite0-dev libxcb-res0-dev libxcb-errors-dev libxcursor-dev \[m
         glslang-dev glslang-tools wayland-protocols xwayland xxd[m
[36m@@ -101,7 +101,8 @@[m [mcp -a "$root/." "$stage/"[m
 # control files as shell scripts.[m
 find "$stage/debian" "$stage/aquamarine-0.12.1/debian" -type f -exec chmod -x {} + 2>/dev/null || true[m
 chmod +x "$stage/debian/rules" "$stage/aquamarine-0.12.1/debian/rules" \[m
[31m-    "$stage/aquamarine-0.12.1/data/hwdata.sh"[m
[32m+[m[32m    "$stage/aquamarine-0.12.1/data/hwdata.sh" \[m
[32m+[m[32m    "$stage/scripts/generateShaderIncludes.sh"[m
 [m
 # The embedded source tree already contains the Anland implementation files,[m
 # while this compatibility change is intentionally delivered only as a quilt[m
[36m@@ -135,13 +136,12 @@[m [msudo apt-get install -y --allow-downgrades "$stage"/libaquamarine11_*.deb "$stag[m
     # Hyprland 0.55.4+ds already uses explicit-safe pointer idioms (for example[m
     # !!pointer) in this restored source baseline.  No out-of-tree hyprutils[m
     # source compatibility patch is required.[m
[31m-    # This Debian packaging helper must be part of CMake before configure;[m
[31m-    # source checkouts without quilt state do not apply it automatically.[m
[31m-    if ! grep -q 'add_executable(dumpabiver debian/dumpabiver.cpp)' CMakeLists.txt; then[m
[31m-        patch --batch -p1 < debian/patches/Add-dumpabiver-abi-helper-to-main-CMakeLists.txt.patch[m
[31m-    fi[m
[31m-    grep -q 'add_executable(dumpabiver debian/dumpabiver.cpp)' CMakeLists.txt[m
[32m+[m[32m    # dpkg-buildpackage invokes dpkg-source, which applies every patch declared[m
[32m+[m[32m    # in debian/patches/series (including the ABI helper) to its private source[m
[32m+[m[32m    # state.  Do not pre-apply a series patch here: doing so creates an[m
[32m+[m[32m    # unrepresentable source change and makes dpkg-source reject the build.[m
     dpkg-buildpackage -us -uc -b[m
[32m+[m
 )[m
 [m
 # Retain only the required runtime packages.  Background, debug and development[m
