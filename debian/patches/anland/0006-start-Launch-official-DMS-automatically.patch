--- a/scripts/start-hyprland-anland
+++ b/scripts/start-hyprland-anland
@@ -48,18 +48,56 @@
     children="$children $!"
 fi

-# DMS is opt-in.  The dedicated launcher is responsible for its user/runtime
-# isolation and waits for the Wayland socket after Hyprland creates it.
-if [ "${ANLAND_DMS:-0}" = 1 ]; then
-    if command -v start-anland-dms >/dev/null 2>&1; then
-        start-anland-dms >/dev/null 2>&1 &
-        children="$children $!"
-    else
-        echo "start-hyprland-anland: ANLAND_DMS=1 but start-anland-dms is unavailable" >&2
-    fi
-fi
+# DMS is supplied by the official AvengeMedia package. It is the session
+# shell: its normal Logout must end Hyprland too, and Hyprland shutdown must
+# terminate DMS helpers together with the DMS process group.
+start_dms() {
+    dms_pid=""
+    cleanup_dms() {
+        if [ -n "$dms_pid" ]; then
+            kill -TERM -- "-$dms_pid" 2>/dev/null || true
+            wait "$dms_pid" 2>/dev/null || true
+        fi
+    }
+    trap cleanup_dms EXIT HUP INT TERM
+
+    i=0
+    while [ "$i" -lt 100 ]; do
+        for socket in "$XDG_RUNTIME_DIR"/wayland-*; do
+            [ -S "$socket" ] || continue
+            for instance_dir in "$XDG_RUNTIME_DIR"/hypr/*; do
+                [ -d "$instance_dir" ] || continue
+                signature=${instance_dir##*/}
+                setsid env \
+                    WAYLAND_DISPLAY="${socket##*/}" \
+                    HYPRLAND_INSTANCE_SIGNATURE="$signature" \
+                    QT_QPA_PLATFORM=wayland \
+                    dms run --session &
+                dms_pid=$!
+                if wait "$dms_pid"; then
+                    dms_status=0
+                else
+                    dms_status=$?
+                fi
+                dms_pid=""
+                # DMS Logout is the requested end of this desktop session.
+                kill -TERM "$hyprland_pid" 2>/dev/null || true
+                return "$dms_status"
+            done
+        done
+        i=$((i + 1))
+        sleep 0.05
+    done
+    echo "start-hyprland-anland: DMS was not started because the Hyprland session was not ready" >&2
+}

 /usr/bin/start-hyprland --anland "$@" &
 hyprland_pid=$!
+if command -v dms >/dev/null 2>&1; then
+    start_dms &
+    children="$children $!"
+else
+    echo "start-hyprland-anland: official dms package is unavailable; starting compositor only" >&2
+fi
 wait "$hyprland_pid"
 exit $?
\ 文件末尾没有换行符
