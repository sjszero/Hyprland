From: Anland Hyprland Maintainers
Date: Fri, 8 Aug 2026 12:20:00 +0800
Subject: [PATCH] compat: explicitly convert XWayland listener pointer to bool

Forky hyprutils uses an explicit CSharedPointer boolean conversion.

--- a/src/xwayland/XSurface.cpp
+++ b/src/xwayland/XSurface.cpp
@@ -78,7 +78,7 @@ void CXWaylandSurface::handleWMClass(const std::string& prop) {
 }
 
 void CXWaylandSurface::ensureListeners() {
-    bool connected = m_listeners.destroySurface;
+    bool connected = static_cast<bool>(m_listeners.destroySurface);
 
     if (connected && !m_surface) {